<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css"
        integrity="sha384-B0vP5xmATw1+K9KRQjQERJvTumQW0nPEzvF6L/Z6nronJ3oUOFUFpCjEUQouq2+l" crossorigin="anonymous">
    <title>Productos</title>

</head>

<body>
    <div class="container mt-3">
        <div class="jumbotron">
            <h2 style="color:crimson;">Lista de productos</h2>
            <% if(data.length > 0){ %>
                <div class="table-responsive">
                    <table class="table table-dark">
                        <tr style="color: yellow;"> 
                            <th>Title</th> 
                            <th>Price</th> 
                            <th>thumbnail</th> 
                        </tr>
                        <% for(const prod of data) {%>
                            <tr>
                                <td><%=prod.title%></td>
                                <td><%=prod.price%></td>
                                    <td><img src="<%=prod.thumbnail%>" alt="" width="50" height="50"></td>
                            </tr>
                        <%};%>
                    </table>
                </div>
            <% } else{ %>
                <h3 class="alert alert-danger">No hay productos cargados</h3>
            <% } %>
            <button class="btn btn-primary mt-3 mb-5" onclick="location.href='./'">Volver al form</button>
        </div>
    </div>
</body>

</html>